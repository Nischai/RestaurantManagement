<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nischaipyda.restaurant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".customer.CustomerActivity" />
        <activity android:name=".staff.StaffActivity" />
        <activity android:name=".staff.StaffAuthActivity" />
        <activity android:name=".staff.CleanerActivity" />
        <activity android:name=".staff.cook.CookActivity" />
        <activity android:name=".staff.ServerActivity"></activity>
    </application>

</manifest>